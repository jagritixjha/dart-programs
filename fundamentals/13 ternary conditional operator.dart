/*
  >> Ternary C onditional Operator
  syntax: 
  condition ? expression1 : expression2;
*/

void main() {
  int age = 18;
  age = 14;
  String elegibility =
      age >= 18 ? 'Eleigible to vote!' : 'Not eligible to vote!';
  print(elegibility);
}
