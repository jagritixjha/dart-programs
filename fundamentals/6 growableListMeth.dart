int main() {
  List<int> arr = [1, 2, 3, 4, 5];
  print(arr);

  // --------------------------   list constructors1
  print(arr.runtimeType);
  print(arr.length);
  print(arr.reversed);
  print(arr.first);
  print(arr.last);
  print(arr.indexed);
  print(arr.isEmpty);
  print(arr.isNotEmpty);
  print(arr.lastOrNull);
  // print(arr.single);
  print(arr.singleOrNull);
  // var x = arr.iterator;
  // print(x);
  print(arr.hashCode);
  return 0;
}
